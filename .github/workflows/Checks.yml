name: Checks

on:
  push:
    branches: [ "main"]
  pull_request:
    branches: [ "main"]


jobs:
  Linter:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v2
        - uses: docker://github/super-linter:v2.1.0
      
  
